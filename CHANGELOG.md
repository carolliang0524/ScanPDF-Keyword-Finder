# 更新记录

## v1.1.0 — 2026-09-04

### 主要升级

- 将双引擎串行执行升级为 Tesseract 与 PaddleOCR 两个独立进程并行执行。
- 同一 PDF 页面只进行一次无损渲染，由两个模型读取相同的只读像素副本。
- 文件完成采用“双引擎会合”规则：两个模型都完成并保存结果后，文件进度才增加。
- 新增网页双模型状态卡，分别展示当前页、状态、耗时、心跳和二次精查。
- 实时命中改为双引擎原始结果中立汇总；完整结束后自动显示最终 merge 结果。
- 增加网页专用原子状态文件，避免从大量日志中猜测进度或继承另一模型的旧进度。
- 完善 Tesseract 探测，兼容 C/D 盘标准位置、用户目录、Conda、PATH 和便携目录。
- 统一复用 `cma_ocr27_web` 环境，不修改 base，不为每个版本重复创建环境。

### 保留能力

- 默认 400 DPI 无损渲染。
- 英文大小写忽略、空格断裂和常见 OCR 混淆容错。
- 疑似页 500 DPI 二次精查。
- 两套原始命中分别保存，最终结果不区分主次地合并去重。
- 可信命中与模糊候选分开导出。
- 断点续跑、心跳、文件名和页码错误日志、XLSX 导出及 PDF 回查。

### 兼容性

- Python 3.10
- PaddlePaddle 2.6.2
- PaddleOCR 2.7.0.3
- NumPy 1.23.5
- Streamlit 1.39.0

### 升级提醒

- v1.1.0 的网页程序依赖新版主程序发布的并行状态文件，请整包升级。
- 不建议只把 `cma_web_app_parallel.py` 复制到 v1.0.0 文件夹。
- 新旧版本应使用不同的结果目录，避免旧断点状态与新版任务混用。
- v1.0.0 Release 保留，可用于回退。

## v1.0.0 — 2026-09-03

- 首个 GitHub 发行版。
- 双引擎串行增强识别。
- 本地 Streamlit 网页、结果筛选、XLSX 导出和 PDF 回查。
- 隔离 Conda 环境和非商业许可证。

